[project]
name = "auto-rl"
version = "0.1.0"
description = "A naive playground to test RL for delivery optimization."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.3.4",
    "tinygrad>=0.11.0",
]
